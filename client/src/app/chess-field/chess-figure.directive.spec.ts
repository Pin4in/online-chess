import { ChessFigureDirective } from './chess-figure.directive';

describe('ChessFigureDirective', () => {
  it('should create an instance', () => {
    // const directive = new ChessFigureDirective();
    // expect(directive).toBeTruthy();
  });
});
